{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "index",
            "request": "launch",
            "command": "node src/index.js",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env"
        },
    ]
}